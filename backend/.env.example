PORT=3001
NODE_ENV=development

# DB (matches docker-compose)
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=mco
DB_PASS=mco
DB_NAME=orchestrator

# Encryption key for future (AES-256-GCM) — generate 32 bytes and base64
ENCRYPTION_KEY_BASE64=CHANGE_ME_BASE64_32_BYTES

# Reserved for later
JWT_SECRET=change_me
